# MySQL Remote Connection and CRUD Scripts

## Description
This repository contains Python scripts for connecting to a remote MySQL database and performing CRUD operations (Create, Read, Update, Delete).  
The scripts also verify the success of each operation.

### Note
In the scripts, you must either replace the connection parameter values with your own or create your own configuration file and place it in this directory.
